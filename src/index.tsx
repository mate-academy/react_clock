import ReactDOM from 'react-dom';
// eslint-disable-next-line import/no-named-as-default
import App from './App';

ReactDOM.render(
  <App isClockVisible clockName={0} />,
  document.getElementById('root'),
);
