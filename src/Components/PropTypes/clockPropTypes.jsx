import PropTypes from 'prop-types';

export const timePropTypes = {
  isVisible: PropTypes.bool.isRequired,
};

export const clockPropTypes2 = {
  show: PropTypes.func.isRequired,
  hide: PropTypes.func.isRequired,
};
